---
batch_connect:
  template: "basic"
script:
  native:
    - "-p"
    - "<%= ENV['RELION_PARTITION'] || 'compute' %>"
    - "-c"
    - "<%= bc_num_slots.to_i %>"
    - "-t"
    - "<%= bc_num_hours.to_i %>:00:00"
    - "--mem"
    - "<%= ENV['RELION_MEM_PER_JOB'] || '12G' %>"
